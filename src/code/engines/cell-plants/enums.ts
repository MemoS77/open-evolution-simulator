export enum Direction {
    Up,
    Down,
    Left,
    Right
}

export enum BotCellKind {
    // Стволовая клетка
    Stem,
    // Листовая клетка
    Leaf,
    // Рот
    Mouth,
    // Репродуктивная клетка
    Reprod,
}