export enum CellActionKind {
    Idle,
    MainAction,
    Move,
    TurnLeft,
    TurnRight,
}



export enum BotKind {
    // Стволовая клетка
    Stem,

    // Листовая клетка
    Leaf,

    // Броня/рот/шип
    Armor,


}